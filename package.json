{
  "name": "sword-x-staff-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "~6.0.2",
    "vite": "^8.1.0",
    "vitest": "^4.0.0"
  },
  "dependencies": {
    "phaser": "^4.2.0"
  }
}
